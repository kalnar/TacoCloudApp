spring:
  application:
    name: TacoCloudApp
  datasource:
    generate-unique-name: false
    name: tacocloud
  data:
    mongodb:
      host: localhost
      port: 27017
      database: tacoclouddb
  thymeleaf:
    cache: false